import { RandomGradientDirective } from './random-gradient.directive';

describe('RandomGradientDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomGradientDirective();
    expect(directive).toBeTruthy();
  });
});
